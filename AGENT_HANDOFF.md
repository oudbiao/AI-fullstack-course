# Agent Handoff Notes

This file is for future agents. It is not course content.

## Operating rules

- Do not push.
- Prefer small local commits that are easy to review.
- Before every commit, run a build check first.
- Do not overwrite user changes.
- Do not commit generated `__pycache__` files.

## Core workflow

1. Read the relevant chapter text line by line before changing any image.
2. Treat images as teaching material, not decoration.
3. Keep English docs, `zh-Hans`, and `ja` aligned in structure, meaning, and image references.
4. If a page or section has a broken image reference, fix the asset or the reference before continuing.
5. After edits, run validation, build, and browser QA.

## Image generation rules

- Use `scripts/generate_course_images.py` for image work.
- The script already reads `.env.local`.
- Available API keys are read from:
  - `OPENAI_API_KEY`
  - `OPENAI_API_KEY_2`
  - `OPENAI_API_KEY_3`
- Prefer parallel generation when the script supports it. Treat each API key as one worker lane: one active image request per key. When a key finishes its image, that same lane should immediately take the next queued image; do not wait for all keys in the batch to finish before starting the next work item.
- For long jobs, allow a long timeout budget (about 1200s).
- Prefer direct AI-generated teaching images.
- Do not fake the result by generating a background and then pasting text on top.
- Do not use SVG-style infographic aesthetics for replacement teaching images.
- Prefer hand-drawn classroom notes / lined notebook / whiteboard teaching styles when the topic fits.
- If a topic is too abstract, use an analogy, sequence, state change, comparison, or error-repair image.
- If one vertical image is too crowded, split the concept into two or more vertical images.
- Prefer vertical teaching images for core lessons, especially step-by-step flows, phone-first reading, and hard concepts that need stacked explanation.
- Vertical image work must be a real vertical recomposition, not a stretched landscape image. Reject images with tall/narrow/compressed lettering, warped circles, squeezed tables, distorted arrows, or text made unreadable to fit the page.
- Generated images must stay in a temporary review location until they pass QA. Only replace course assets after checking dimensions, readability, language, layout pressure, concept match, and absence of stretching/warping.
- Image generation is a candidate step, not acceptance. A generated file is not allowed into `static/img/course` or Markdown references until it passes QA.
- For every generated or regenerated course image, inspect it before adoption. At minimum check: correct aspect ratio, no forced stretch, readable large text, natural language for the locale, no tiny filler/gibberish, no fake brand/logo/watermark, no invented metrics or output values, and a clear match to the nearby lesson/code/output.
- If the image fails QA, regenerate or simplify. Do not “settle” for stretched text, crowded diagrams, decorative posters, wrong language, hallucinated numbers, or unrelated imagery.
- If a concept is too dense for one vertical page, split it into two images or reduce visible text. Never solve density by shrinking or distorting text.
- Result-map images must not invent values. They must match the real code/output shown nearby, or use nonnumeric schematic labels when the page does not provide exact numbers.
- Chapter/project route images should not look like marketing posters. Prefer reviewable teaching artifacts: route maps, evidence chains, debugging order, before/after comparison, output-reading notes, or project checklists.
- For abstract or hard-to-remember ideas, prefer analogy images that make the mental model visible before the formal terms.
- For important historical turning points, algorithm evolution, or "why this changed the field" moments, use multi-panel hand-drawn comics when they improve memory and pacing.
- Keep the image content tightly tied to the chapter context and the actual code/output shown nearby.
- Do not change outputs just to make the image easier to draw.
- If Chinese text is used, keep it natural Chinese. Allow English terms only when they are real technical terms.
- For multilingual image sets, keep the visual style consistent across EN, ZH, and JA.

## Course structure rules

- Keep the default learner route embedded in the course flow rather than relying on learners to read a separate recommended-route page first.
- Do not move major chapters unless explicitly requested. The current default route is tools -> Python -> data -> math -> ML -> DL/Transformer -> LLM -> RAG -> Agent -> specializations.
- Chapter index pages should act as navigation stations: what the learner already knows, what this chapter solves, the core path, optional extensions, depth challenge, evidence to keep, and the next chapter bridge.
- For model-foundation chapters 4-6, keep the learning practical: runnable code, visible output, metrics/curves/errors, and a bridge into Chapter 7 LLMs.
- Treat "beginner-friendly" as "easy first action, clear evidence, and recoverable failure," not shallow content.
- For Chapters 6-9, preserve the core/extension split in sidebar labels and chapter indexes:
  - Chapter 6 core path: 6.1 -> 6.2 -> 6.5 -> 6.8.
  - Chapter 7 core path: 7.1 -> 7.2 -> 7.5 -> 7.8.
  - Chapter 8 core path: 8.1 -> 8.3 -> 8.4 -> 8.5, with deployment/API serving as an extension.
  - Chapter 9 core single-Agent path: 9.1 -> 9.2 -> 9.3 -> 9.4 -> 9.8 -> 9.10; MCP, frameworks, multi-agent, and deployment operations are advanced/elective after the single-Agent loop is stable.

## Quality bar

- Text must be readable.
- No garbled small text.
- No random decorative boxes or pure poster layouts.
- No image that feels unrelated to the surrounding explanation.
- If a generated image feels too close to an infographic or text-only poster, regenerate it.
- If a chart, algorithm, or workflow can be clarified with an actual diagram or step-by-step visual, prefer that.

## Verification

- Run image reference validation after image changes.
- Run `npm run build`.
- If relevant, run the Docker build flow too.
- Open the local site in the browser and inspect the affected chapters directly.
- Check mobile layout, image height, text pressure, chapter pacing, and language switching.

## Current optimization status

- 2026-05-19: Intro, Chapters 1-12, electives, and appendix have been optimized across English, Simplified Chinese, and Japanese.
- 2026-05-19: Chapter 6 vertical image refinement is complete and committed as `f7b70382` (`Refine ch06 content and vertical course images`). QA report and handoff are in `reports/course-images/ch06-vertical-refine/`; 30 approved candidate PNGs were converted to official `static/img/course/*.webp` assets.
- 2026-05-19: Chapter 7 refinement is complete and committed as `13df18b2` (`Refine ch07 LLM principles content`). English, Simplified Chinese, and Japanese `ch07-llm-principles` each have 44 pages with image references, code blocks, evidence sections, and expected-output/result cues. Image references are WebP-only and all referenced course assets exist.
- 2026-05-19: Chapter 8 refinement is complete and committed as `b138a62c` (`Refine ch08 RAG application content`). English, Simplified Chinese, and Japanese `ch08-rag` each have 34 pages with image references, code blocks, evidence sections, and expected-output/result cues. Image references are WebP-only and all referenced course assets exist.
- 2026-05-19: Chapter 9 refinement is complete. English, Simplified Chinese, and Japanese `ch09-agent` each have 70 pages with image references, code blocks, evidence sections, and expected-output/result cues. Image references are WebP-only and all referenced course assets exist. The chapter index preserves the core single-Agent route `9.1 -> 9.2 -> 9.3 -> 9.4 -> 9.8 -> 9.10`, with MCP, frameworks, multi-agent, and deployment operations treated as advanced/elective after the single-Agent loop is stable. Local ch09 audit plus full repository validation/build passed.
- 2026-05-19: Chapter 7-9 vertical image refinement resumed after content commits. Inventories are written to `reports/course-images/ch07-vertical-refine/`, `reports/course-images/ch08-vertical-refine/`, and `reports/course-images/ch09-vertical-refine/`. Current candidate counts from official Markdown WebP refs: ch07 has 45 non-vertical assets, ch08 has 123, ch09 has 153; missing official assets are 0. Candidate PNGs must be generated under `tmp/ch07-vertical-images/`, `tmp/ch08-vertical-images/`, and `tmp/ch09-vertical-images/`, QA'd, then converted to same-stem WebP in `static/img/course/`.
- 2026-05-20: Continued zh-Hans/ja visible-title and list cleanup after the user clarified `(Extension) Core Evidence` was only an example. Fixed clear learner-facing half-translation residues across ch07-ch09, including `de编码者` / `Deコーダー`, `上下文ual` / `コンテキストual`, `步骤s` / `ステップs`, `路线r`, `トレースs`, and visible Mermaid labels such as Planner/Executor/Trace/Metrics. Markdown image URLs, evidence-card field names, code identifiers, and front matter keywords were preserved. A structure-aware scan over zh-Hans/ja ch07-ch09 headings, lists, tables, and Mermaid node labels now reports `NO_HITS` for the targeted residue patterns. Next: run the full validation/build chain and commit this content-cleanup slice; ch09 image QA/conversion remains pending.
- 2026-05-19: Chapter 7 vertical image refinement is complete. All 45 candidate PNGs in `tmp/ch07-vertical-images/` passed QA and were converted to official same-stem WebP files in `static/img/course/`; QA details are in `reports/course-images/ch07-vertical-refine/qa-report.md`. Validation and `npm run build` passed before commit. Continue with ch08 candidate generation and QA.
- 2026-05-19: ch08 vertical image generation was paused after 19 zh/base candidate PNGs to address localized sidebar/title English residue. Added zh-Hans and ja translations for the new ch06-ch09 sidebar labels with `(Core)`, `(Extension)`, `(Deep Dive)`, `(Core Evidence)`, and related path tags. Also localized visible zh/ja table rows such as "Advanced RAG", "Advanced planning", and "Electives" where they appeared as learner-facing list labels. Custom scans now report 0 missing zh-Hans/ja sidebar translations for those suffixed labels and 0 visible localized message/title hits for the English taxonomy residue. Resume ch08 image generation after validation/commit.
- 2026-05-19: After the user clarified that `(Extension) Core Evidence` was only one example, an additional rendered-content scan was run across zh-Hans and ja. `current.json` learner-facing `message` values plus Markdown headings, front matter labels, list rows, and table rows now report 0 hits for the English taxonomy residue patterns.
- 2026-05-19: ch08 vertical image QA is in progress. All 123 zh/en/ja candidate PNGs exist at `1024x1792` under `tmp/ch08-vertical-images/`. High-risk candidates with invented tool outputs or brand-like icons were regenerated after hardening the image prompts. No ch08 official WebP files have been replaced yet; final visual QA, QA report, conversion, validation/build, and commit remain pending.
- 2026-05-19: ch08 final visual QA rejected and regenerated the remaining risky state/four-layer study guide candidates that had fake invoice/metric values or brand-style deployment icons. All 123 ch08 candidate PNGs now pass dimension and visual QA; write/commit the QA report, convert accepted PNGs to official WebP, then validate/build before continuing ch09.
- 2026-05-19: Chapter 8 vertical image refinement is complete and committed as `ce710626` (`Refine ch08 vertical course images`). All 123 accepted ch08 candidate PNGs were converted to official same-stem WebP files in `static/img/course/`; QA details are in `reports/course-images/ch08-vertical-refine/qa-report.md`. Required validators, `git diff --check`, and `npm run build` passed before commit. Continue with ch09 candidate generation and QA.
- 2026-05-19: Follow-up localization sweep widened beyond the earlier example strings. Visible zh-Hans/ja title and list-label residues such as `readiness`/`production` in the ch09 deployment best-practices pages were localized, and `demo -> production` was fixed in the ja ch08 inference-server page. The remaining scan hits are mostly technical terms, code identifiers, or data in code blocks rather than learner-facing titles. Continue with ch09 vertical image QA, then convert approved PNGs to WebP and run the validation/build chain.
- 2026-05-19: New localized-title sweep focused on visible zh-Hans/ja headings, table rows, and list labels in ch07-ch09 plus intro. Localized `Step` headings into `步骤` / `ステップ`, replaced several visible `demo` labels with localized wording, and cleaned a few sidebar-adjacent table rows. Current remaining hits are mostly code/example strings, such as model IDs or fenced sample output, rather than learner-facing titles. Keep the sweep narrow and preserve technical identifiers inside code blocks.
- 2026-05-19: Follow-up zh-Hans/ja visible-text sweep expanded beyond the user's example. Learner-facing natural-language `Demo/demo` residues were localized across the i18n docs (`演示` / `デモ`) while preserving code identifiers, filenames, model IDs, and fenced examples. A custom scan that excludes fenced code blocks and inline code now reports 0 visible `Demo/demo` hits in zh-Hans and ja. The taxonomy suffix patterns such as `(Extension)`, `(Core Evidence)`, `(Deep Dive)`, `(Core Bridge)`, `(Core App Shell)`, and `(Advanced / Elective)` also report 0 Markdown hits; `current.json` still contains English source keys/descriptions, but the displayed `message` values are localized. Continue ch09 vertical image QA/conversion next.
- 2026-05-20: Follow-up zh-Hans/ja visible-label sweep for ch07/ch08/ch09 cleaned learner-facing English title/list/table residues beyond the earlier examples. Localized HHH labels, Prompt/Function Calling/Agent/RAG rows, zero/few-shot rows, RAG evaluation dimensions, inference latency/throughput headings, dialog state labels, MCP client/server/tool/transport/resources/prompts headings, LlamaIndex/CrewAI/OpenAI Agents SDK concept headings, multi-agent role labels, and ch09 project/study-guide labels. Fenced code, inline code, schema fields, error codes, model IDs, API/library names, and real technical method names remain intentionally unchanged. Custom scans now report `heading_hits=0` for visible zh-Hans/ja headings in ch07-ch09 and 0 targeted hits for the example-style labels (`Planner`, `Reviewer`, `Tool schema`, `Correctness`, `Endpoint`, `Payload`, etc.) outside code. Required validators, `git diff --check`, and `npm run build` passed. Continue ch09 vertical image QA/conversion next.
- 2026-05-20: Follow-up scan confirmed the sidebar/title example was representative, not a one-off. Rechecked zh-Hans and ja visible Markdown, tables, lists, and sidebar message values for `Core Evidence`, `Extension`, `Deep Dive`, `Goal`, `Verify`, `Review`, `Checkpoint`, `Metrics`, and similar display labels. No new visible hits remained outside fenced code or inline code, and the translated sidebar messages in `current.json` already cover the chapter group labels. ch09 candidate PNG QA is complete and ready for WebP promotion; next step is conversion to official `static/img/course/*.webp`, then the required validators/build and commit.
- 2026-05-20: Follow-up user clarification: the `(Extension) Core Evidence` example was representative, so I rechecked zh-Hans and ja visible headings, list rows, tables, and sidebar labels for the broader class of English titles. A narrow residual scan found only deliberate technical acronyms / code identifiers such as `HHH` and the expected model/API names; no further reader-facing title cleanup was needed for ch07-ch09. ch09 candidate PNGs were then promoted to official WebP assets and the QA report was finalized. Next step is the required validators/build and commit.
- 2026-05-20: User clarified that `(Extension) Core Evidence` was only an example and asked to check all similar visible English titles in zh-Hans and Japanese. I re-scanned ch07-ch09 learner-facing headings, lists, tables, and figure labels, then localized the remaining clear residues such as `public benchmarks`, `tool/function calling`, and the zh/ja benchmark figure label. Technical names and code examples were left intact. Next step is to keep the visible-label sweep narrow, rerun validation, then continue ch09 vertical image QA/conversion.
- 2026-05-20: Continued the broader zh-Hans/ja visible-label sweep after the user clarified the English title issue was not limited to one example. Localized obvious learner-facing residues in ch07 workbench tables, ch08 RAG evaluation rows, ch09 system architecture labels, multi-Agent pattern/practice pages, the multi-Agent dev-team project, and the single-Agent workshop tables/checklists. A follow-up scan still shows visible lowercase role-name residues in ch09 multi-Agent communication/coordination/challenges, plus a few study-guide/evaluation labels; finish those next, then rerun validation/build. ch09 image status is unchanged: 153 candidate PNGs still need visual QA, QA report, WebP conversion, validation/build, and commit.
- 2026-05-20: Continued the visible English title/list sweep in zh-Hans and ja. Localized remaining learner-facing labels in ch09 AutoGen and observability pages, including visible role names in prose/list items and `Trace`/`Replay`/role-name labels in diagrams, tables, alt text, and tips. Code identifiers, schema fields, JSON/log filenames, and fenced output examples were preserved. Next step: rerun a broader ch07-ch09 visible-line scan for zh-Hans/ja, patch only true reader-facing residues, update handoff again, then run validators/build before any commit. ch09 images remain candidates only.
- 2026-05-20: Follow-up zh-Hans/ja title sweep handled the broader example class the user pointed out. Learner-facing Markdown/frontmatter headings that were still raw English phrases now use local-language names with the English term in parentheses where useful, including Causal/Masked LM, Span Corruption, Prompt/Prefix Tuning, Constitutional AI, Agentic/Graph RAG, Exact Match, Function Calling, One-Hot/Dense Vector, LLM API, Tiny RAG, Research Assistant Agent, and Japanese `Neural Models` / `Trace`. Remaining heading scan hits are intentionally preserved brand/product/model/API names such as Hugging Face, OpenAI Responses API, AI Agent, TD-Gammon, DQN Atari, OpenAI Agents SDK, and SDK API. ch09 image status is unchanged: 153 candidate PNGs still need visual QA, `qa-report.md`, WebP conversion, validation/build, and a commit.
- 2026-05-20: ch09 vertical image QA is now in progress across all 153 candidate PNGs. Automatic dimension checks passed for all candidates at `1024x1792`, and OCR plus contact-sheet review found a small rejected set that must be regenerated before any WebP promotion: fake secret/timestamps in `ch09-agent-security-prompt-injection-risk-map.png`, concrete timestamps/ids in `ch09-agent-persistence-checkpoint-eventlog-map*` and `ch09-multi-agent-communication-contract-map-ja.png`, fictional citation authors/years/pages/percentages in `ch09-research-assistant-citation-trace-map*`, invented notebook/table/test/dashboard values in `ch09-data-analysis-agent-notebook-loop-map*`, `ch09-code-agent-sandbox-review-map*`, and `ch09-project-delivery-loop-en.png`, plus fake dates/importance values in Japanese memory candidates. No ch09 official WebP files have been replaced yet.
- 2026-05-20: ch09 rejected candidate regeneration pass completed. Added `CH09_VERTICAL_REFINEMENT_PROMPTS` in `scripts/generate_course_images.py` to force abstract placeholders and ban real-looking secrets, dates, ids, citations, metrics, tables, logs, and dashboard values for risky ch09 stems. Regenerated 20 candidate PNGs under `tmp/ch09-vertical-images/`: 6 zh/base, 6 en, and 8 ja targets covering security, persistence, code-agent, data-analysis, reasoning-eval, research-citation, project-delivery-en, memory-ja, and multi-agent-communication-ja. Mechanical checks now show all 153 ch09 candidate PNGs at `1024x1792`; post-regeneration OCR risk hits are residual false positives from step badges/icons after contact-sheet and full-size review. No official `static/img/course/*.webp` files have been replaced yet. Next: write `reports/course-images/ch09-vertical-refine/qa-report.md`, convert all approved PNGs to same-stem WebP, run the required validation/build chain, and commit the final ch09 image promotion.
- 2026-05-20 14:04 CST: User clarified that current homework/exercise questions can be modified, added, or deleted as needed for teaching quality. New active workstream: add default-collapsed answer/explanation blocks to chapter-end exercises, starting with the ch06-ch09 core learner path and keeping EN/zh-Hans/ja synchronized. Scope for the first progress chunk is the main route pages only, not all 410 existing exercise headings at once. Do not mechanically append answers without checking whether the exercise itself is still pedagogically useful; revise weak questions where needed.
- 2026-05-20 14:18 CST: ch06 core-path exercise refinement chunk is complete. Added default-collapsed reference answer/explanation blocks to 13 English ch06 core-path exercise sections and synchronized the same treatment in zh-Hans and ja, for 39 `<details>` blocks total. Covered ch06 core route pages in `ch01-nn-basics/05-weight-init.md`, all `ch02-pytorch` pages, `ch05-transformer/01-02`, and ch06 project pages `01-03`. Kept technical terms such as `CrossEntropyLoss`, `BCEWithLogitsLoss`, `GQA/MQA`, `state_dict()`, `Dropout`, and `device` in English where useful while localizing learner-facing explanations. Validation passed after the chunk: markdown fences, internal links, sidebars, course structure, course image refs, `git diff --check`, and `npm run build`. Next: repeat the same exercise-answer workflow for ch07 core path.
- 2026-05-20 14:42 CST: ch07 main learner path exercise-answer chunk is drafted. Added default-collapsed answer/explanation blocks to 15 English pages and synchronized zh-Hans/ja, for 45 `<details>` blocks total. Covered `ch01-nlp-crash/01-05`, `ch02-llm-overview/01-04`, `ch05-prompt/01-05`, and `ch08-projects/01-domain-finetuning.md`; this intentionally includes the LLM call workbench and tokenizer/prompt evaluation lab pages discovered during the exercise-heading scan. Structure check passed locally: each target page has balanced `<details>` tags and exactly one language-appropriate summary (`Reference answers and explanation`, `参考答案与讲解`, `参考解答と解説`). Next: run the required validators/build, commit this chunk, then continue ch07 remaining deep/advanced exercise pages before moving to ch08.
- 2026-05-20 15:18 CST: ch07 remaining deep/advanced exercise-answer chunk is drafted. Added default-collapsed answer/explanation blocks to 15 English pages and synchronized zh-Hans/ja, for 45 new `<details>` blocks total. Covered Transformer deep dives `ch03-transformer-deep/01-04`, pretraining `ch04-pretraining/01-03`, finetuning `ch06-finetuning/01-05`, and alignment `ch07-alignment/01-03`. Exercises were kept because they already ask useful parameter changes, mechanism explanations, and project judgments; answer blocks now add expected observations, decision criteria, and common failure modes. Structure check passed locally: each target page has balanced `<details>` tags and exactly one language-appropriate summary. Next: run the required validators/build, stage only these ch07 files plus this handoff, commit, then move to ch08 exercise-answer work.
- Latest pass focused on Chapters 1-5. Every page in `ch01-tools`, `ch02-python`, `ch03-data-analysis`, `ch04-ai-math`, and `ch05-machine-learning` now has a localized evidence section, image reference, code block, and expected output/result cue.
- Chapter index pages for Chapters 1-5 now include output-reading guidance so beginners know how to interpret terminal output, files, metrics, and plots.
- Latest verification passed: markdown fences, internal links, sidebars, course structure, image refs, `git diff --check`, and `npm run build`. Image audit reported `course_image_refs=2957 missing=0 unused=0`.

## Practical notes

- When replacing old SVG assets, remove unused SVGs only after confirming nothing references them.
- Keep report and script changes separate from course content edits when possible.
- If a change needs a follow-up commit, prefer a second small commit over one large mixed commit.
